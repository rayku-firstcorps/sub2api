# 关键词过滤 — 宽松/严格模式 PRD

## 1. 背景与动机

当前关键词过滤系统采用**全量扫描**策略：对请求中所有 user 消息（含历史对话）进行关键词匹配，一旦命中即拦截。这在多轮对话场景下存在问题——用户首次发送的提示词被拦截后，如果修改了内容重新发送，历史消息中仍可能包含之前被拦截的内容（由客户端缓存或上下文管理），导致持续误拦。

**核心需求：** 提供两种过滤模式，让管理员根据业务场景灵活选择拦截力度。

---

## 2. 模式定义

### 2.1 严格模式（Strict）— 当前默认行为

| 维度 | 说明 |
|------|------|
| 扫描范围 | 请求体中**所有** user 角色消息（含历史对话） |
| 拦截逻辑 | 任意消息命中关键词即拦截整个请求 |
| 适用场景 | 高合规要求场景，需确保上下文中不含任何违规内容 |
| WebSocket | 维持滑动窗口全量检测（不变） |

### 2.2 宽松模式（Lenient）

| 维度 | 说明 |
|------|------|
| 扫描范围 | 仅扫描请求中**最后一条** user 角色消息（即当前轮次用户输入） |
| 拦截逻辑 | 仅当前轮次用户输入命中关键词时拦截；历史消息中的命中不触发拦截 |
| 适用场景 | 多轮对话场景，允许历史上下文中存在曾被拦截的内容，只要当前输入合规即放行 |
| WebSocket | 仅检测当前帧/当前滑动窗口内容（不变，WebSocket 本身就是增量检测） |

---

## 3. 配置设计

### 3.1 新增配置字段

在 `KeywordFilterConfig` 中新增：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `filter_mode` | string | `"strict"` | 过滤模式：`"strict"` 或 `"lenient"` |

### 3.2 API 变更

**UpdateConfig 请求体新增字段：**

```json
{
  "filter_mode": "lenient"
}
```

**GetConfig 响应新增字段：**

```json
{
  "filter_mode": "strict"
}
```

### 3.3 数据模型变更

```go
type KeywordFilterConfig struct {
    // ... 现有字段 ...
    FilterMode string `json:"filter_mode"` // "strict" | "lenient"
}

type UpdateKeywordFilterConfigInput struct {
    // ... 现有字段 ...
    FilterMode *string `json:"filter_mode"`
}
```

合法值校验：仅接受 `"strict"` 和 `"lenient"`，其他值返回 400。

---

## 4. 核心逻辑变更

### 4.1 文本提取层（keyword_filter_input.go）

新增函数 `ExtractKeywordFilterLastUserSegments`，各协议仅提取最后一条 user 消息：

| 协议 | 严格模式提取 | 宽松模式提取 |
|------|-------------|-------------|
| OpenAI Chat | 所有 `role=user` 消息 | 最后一条 `role=user` 消息 |
| Anthropic Messages | 所有 user 消息 | 最后一条 user 消息 |
| OpenAI Responses | 所有 input 中的 user 消息 | 最后一条 user 类型 input |
| Gemini | 所有 user contents | 最后一条 user content |

### 4.2 Check 流程变更

```
Check() 入口
  ├─ 加载 runtime snapshot（含 filter_mode）
  ├─ if filter_mode == "lenient":
  │     segments = ExtractKeywordFilterLastUserSegments(protocol, body)
  │  else:
  │     segments = ExtractKeywordFilterSegments(protocol, body)
  ├─ 归一化 + AC 自动机扫描 + 正则扫描
  └─ 白名单豁免 → 决策
```

### 4.3 WebSocket 处理

WebSocket 模式下无需变更——当前已是增量检测（滑动窗口），天然符合宽松模式语义。两种模式下 WebSocket 行为一致。

---

## 5. 前端交互

### 5.1 UI 位置

在关键词过滤配置页面顶部区域（`enabled` 开关下方），新增模式切换：

```
┌─────────────────────────────────────────────┐
│ 关键词过滤                          [开启] │
│                                             │
│ 过滤模式：                                  │
│   ○ 严格模式 — 扫描所有历史消息             │
│   ● 宽松模式 — 仅扫描当前轮次用户输入       │
│                                             │
│ ─────────────────────────────────────────── │
│ 关键词规则 / 正则规则 / 白名单 ...          │
└─────────────────────────────────────────────┘
```

### 5.2 交互说明

- 使用 Radio Button 组，默认选中「严格模式」
- 切换后需点击保存生效（与其他配置项一致）
- 模式切换不影响已有规则配置
- 提供 tooltip 说明两种模式的差异

---

## 6. 日志与审计

### 6.1 日志记录

命中日志新增字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `filter_mode` | string | 命中时的过滤模式 |

用于审计追溯：在宽松模式下被放行的历史消息命中不会产生日志（因为不扫描）。

### 6.2 测试接口

测试接口（Test endpoint）行为不受模式影响——测试时始终对输入文本全量扫描，确保规则验证的准确性。

---

## 7. 兼容性与迁移

| 场景 | 处理方式 |
|------|----------|
| 现有部署升级 | `filter_mode` 默认值为 `"strict"`，行为与升级前完全一致 |
| 配置存储 | 复用现有 JSON 配置存储，新增字段即可 |
| API 兼容 | 不传 `filter_mode` 时保持原值不变（PATCH 语义） |
| 缓存 | runtime snapshot 已包含完整配置，无需额外缓存失效逻辑 |

---

## 8. 边界情况

| 场景 | 行为 |
|------|------|
| 请求中只有一条 user 消息 | 两种模式行为一致 |
| 请求中无 user 消息 | 两种模式均放行（无文本可扫描） |
| system 消息包含关键词 | 两种模式均不扫描 system 消息（现有行为，仅扫描 user 角色） |
| 宽松模式 + 多条连续 user 消息（无 assistant 间隔） | 仅扫描最后一条 user 消息 |
| 配置变更生效延迟 | 受 runtime cache TTL（5s）影响，与现有行为一致 |

---

## 9. 实现优先级

| 阶段 | 内容 | 预估工作量 |
|------|------|-----------|
| P0 | 后端：配置模型 + 文本提取分支 + Check 逻辑 | 0.5d |
| P0 | 后端：配置校验 + API 变更 | 0.5d |
| P1 | 前端：模式切换 UI | 0.5d |
| P2 | 日志字段扩展 | 0.5d |

---

## 10. 验收标准

1. **严格模式**：行为与当前版本完全一致，所有现有测试通过
2. **宽松模式**：多轮对话中，历史消息命中关键词不触发拦截，仅当前轮次输入命中时拦截
3. **模式切换**：切换模式后 5s 内生效（受缓存 TTL 限制）
4. **默认行为**：新部署和升级部署默认为严格模式
5. **测试接口**：不受模式影响，始终全量扫描
6. **WebSocket**：两种模式下行为一致（增量检测）
